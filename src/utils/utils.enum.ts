export enum TicketStatus {
    OPEN = 'open',
    IN_PROGRESS = 'in progress',
    CLOSED = 'closed',
    DELETED = 'deleted',
}

export enum AccountStatus {
    VERIFIED = 'verified',
    NOT_VERIFY = 'not_verify',
    DELETED = 'deleted',
    DISABLED = 'disabled',
}
